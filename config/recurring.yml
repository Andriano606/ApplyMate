# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

fetch_proxies:
  class: Proxy::Job::FetchProxies
  schedule: every day at 0am
# Validates untested proxies (fast pure-Ruby AsyncHttp, no subprocess) and records the
# per-source result — promotes live ones into the "working" tier the sync uses, and keeps
# the pool fresh as proxies churn. Concurrency-limited to 1 (see Proxy::Job::Validate).
validate_proxies:
  class: Proxy::Job::Validate
  schedule: every 5 minutes
#sync_vacancies:
#  class: Vacancy::Job::SyncVacancies
#  schedule: every day at 3am
clear_solid_queue_finished_jobs:
  command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
  schedule: every hour at minute 12
